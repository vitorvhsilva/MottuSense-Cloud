spring:
  application:
    name: users

  datasource:
    url: ${DB_URL:jdbc:sqlserver://mottusensesqlserver.database.windows.net:1433;database=MottuSenseDB;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;}
    username: ${DB_USERNAME:sqladmin}
    password: ${DB_PASSWORD:SenhaForte!123}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    jpa:
      database-platform: org.hibernate.dialect.SQLServerDialect
      properties:
        hibernate.format_sql: true
    # spring.jpa.show-sql: true
    # spring.jpa.hibernate.ddl-auto: create-drop

  port: 8080

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1090689393708-mbmtf324es75n9mh9rmoi8ggtu0j2fli.apps.googleusercontent.com
            client-secret: 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
